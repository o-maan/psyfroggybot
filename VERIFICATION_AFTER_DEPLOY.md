# 🔍 Инструкция по проверке после деплоя

## 1. Проверка что бот запущен

```bash
ssh root@94.228.118.202 "pm2 status"
```

Должно быть: `psyfroggybot | online`

## 2. Проверка логов при инициализации

```bash
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 100 --nostream | grep -E '(timezone|cron|Остановлены|ВАЖНО)'"
```

**Ожидаемые логи:**
- `🌍 Инициализация timezone-based планировщика`
- `👥 Фильтрация пользователей` (должны быть только положительные chat_id)
- `🕐 Создание cron jobs для Europe/Belgrade`
- `✅ Cron jobs созданы` (должно быть `cronCount: 5`)

**НЕ должно быть:**
- Ошибок создания cron jobs
- Дублей сообщений `Создание cron jobs`

## 3. Проверка что старые jobs НЕ создаются

```bash
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 200 --nostream | grep -i 'МСК'"
```

**НЕ должно быть:**
- `Создание morning check cron job (8:00 МСК)`
- `Создание evening batch processing cron job (19:45 МСК)`

Должен быть ТОЛЬКО: `Инициализация автоматического ежедневного расписания`

## 4. Мониторинг вечернего поста (20:00 Belgrade = 22:00 Москва)

**КРИТИЧЕСКИ ВАЖНО:** Проверить в день деплоя, что в 22:00 МСК придет **ТОЛЬКО 1** вечерний пост!

```bash
# Проверка логов в момент отправки (22:00 МСК = 20:00 Belgrade)
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 50 --nostream | grep -E '(Вечерний пост|Evening|batch)'"
```

**Ожидаемые логи в 22:00 МСК:**
- `🌆 Вечерний пост (timezone-based)` - **ТОЛЬКО 1 РАЗ**
- Отправка сообщения в канал
- Отправка первого задания в комментарии

**КРАСНЫЙ ФЛАГ (проблема):**
- Если лог `🌆 Вечерний пост` повторяется 2+ раз
- Если лог `📦 Начало batch обработки` повторяется 2+ раз в одну миллисекунду

## 5. Проверка JOY логики (только в воскресенье)

В воскресенье в 22:00 МСК проверить логи:

```bash
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 100 --nostream | grep -i воскресен"
```

**Ожидается:**
- `📅 Воскресенье - показываем список радости`
- Отправка JOY поста вместо обычного вечернего

## 6. Проверка злой лягухи (8:00 Belgrade = 10:00 Москва)

На следующий день после деплоя в 10:00 МСК:

```bash
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 50 --nostream | grep -E '(Утренняя проверка|checkUsersResponses|злой)'"
```

**Ожидается:**
- `🌅 Утренняя проверка (timezone-based)` - **ТОЛЬКО 1 РАЗ**
- `🔍 Проверка ответов пользователя 5153477378`
- Если пользователь не ответил → отправка злого поста

## 7. Проверка при перезапуске PM2

```bash
ssh root@94.228.118.202 "pm2 restart psyfroggybot && sleep 5 && pm2 logs psyfroggybot --lines 50 --nostream | grep 'Остановлены'"
```

**Ожидается:**
- `🛑 Остановлены старые cron jobs перед пересозданием`

Это доказывает, что при перезапуске старые jobs останавливаются!

## ✅ Критерии успеха

- [ ] PM2 статус: online
- [ ] Timezone-based планировщик инициализирован
- [ ] 5 cron jobs созданы для Europe/Belgrade
- [ ] Старые МСК jobs НЕ создаются
- [ ] Вечерний пост приходит **РОВНО 1 РАЗ** в 22:00 МСК
- [ ] JOY пост работает по воскресеньям (проверить в ближайшее воскресенье)
- [ ] Злая лягуха срабатывает в 10:00 МСК (проверить на следующий день)
- [ ] При перезапуске старые jobs останавливаются

## 🚨 Если что-то пошло не так

### Проблема: Дубли постов всё ещё есть

1. Проверь логи на наличие дублей инициализации:
```bash
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 500 | grep 'Создание cron jobs'"
```

2. Перезапусти PM2:
```bash
ssh root@94.228.118.202 "pm2 restart psyfroggybot"
```

3. Проверь что старые jobs останавливаются:
```bash
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 50 | grep 'Остановлены'"
```

### Проблема: JOY логика не работает в воскресенье

Проверь логи в момент отправки вечернего поста (22:00 МСК в воскресенье):
```bash
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 100 | grep -E '(воскресен|JOY|радост)'"
```

### Проблема: Злая лягуха не отправляется

Проверь что утренняя проверка срабатывает:
```bash
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 100 | grep -E '(Утренняя проверка|checkUsersResponses)'"
```

## 📊 Дашборд для мониторинга

Создай простой скрипт для быстрой проверки:

```bash
#!/bin/bash
echo "=== PM2 Status ==="
ssh root@94.228.118.202 "pm2 status | grep psyfroggybot"
echo ""

echo "=== Последние логи ==="
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 20 --nostream"
echo ""

echo "=== Активные cron jobs (проверка на дубли) ==="
ssh root@94.228.118.202 "pm2 logs psyfroggybot --lines 500 | grep -c 'Создание cron jobs'"
echo "(Должно быть ≤ 2: один при старте, один если был перезапуск)"
```

## 🎯 Главное правило

**После деплоя ОБЯЗАТЕЛЬНО проверь вечерний пост в 22:00 МСК!**

Если придет 1 пост - всё работает корректно ✅
Если придет 2+ постов - есть проблема с дублями ❌
