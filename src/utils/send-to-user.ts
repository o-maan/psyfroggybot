/**
 * Централизованная функция для отправки сообщений пользователю
 *
 * КРИТИЧЕСКИ ВАЖНО: ВСЕГДА используй эту функцию вместо bot.telegram.sendMessage()
 * для отправки сообщений пользователю, чтобы автоматически адаптировать текст под пол
 */

import type { Telegraf } from 'telegraf';
import { getUserByChatId } from '../db';
import { adaptTextForGender } from './gender-adapter';
import { botLogger } from '../logger';

/**
 * Словарь фиксированных текстов с готовыми вариантами для женского рода
 *
 * Структура:
 * - male: текст в мужском роде (базовый вариант)
 * - female: текст в женском роде (готовый адаптированный)
 * - string: одинаковый для обоих родов
 *
 * ВАЖНО: Эти тексты все равно проходят через gender-adapter для дополнительной проверки
 */
type FixedText =
  | { male: string; female: string }
  | string;

const FIXED_TEXTS: Record<string, FixedText> = {
  // ===== НАПОМИНАНИЯ =====

  'reminder_evening_task': {
    male: '! Не забудь ответить на сегодняшнее задание, если еще не успел',
    female: '! Не забудь ответить на сегодняшнее задание, если еще не успела',
  },

  'reminder_unfinished': '🐸 Вижу, что лягуха не получила ответы на все задания. Давай доделаем - возвращайся 🤗',

  'reminder_breathing': {
    male: 'Выполни практику и нажми "Сделал" после ее завершения',
    female: 'Выполни практику и нажми "Сделала" после ее завершения',
  },

  // ===== КНОПКИ И КОРОТКИЕ СООБЩЕНИЯ =====

  'button_finished_writing': {
    male: 'Дописал? Можешь дополнить и тыкай на кнопку 🐸',
    female: 'Дописала? Можешь дополнить и тыкай на кнопку 🐸',
  },

  'before_breathing_practice': {
    male: 'Отличная работа! 🌟 Теперь выполни дыхательную практику и нажми "Сделал" после ее завершения',
    female: 'Отличная работа! 🌟 Теперь выполни дыхательную практику и нажми "Сделала" после ее завершения',
  },

  'joy_empty_list': {
    male: 'Ты еще ничего не написал 🤔\nНапиши, что тебя радует!',
    female: 'Ты еще ничего не написала 🤔\nНапиши, что тебя радует!',
  },

  'joy_add_more': 'Напиши, что еще хочешь добавить ❤️‍🔥',

  'joy_menu': 'Хорошо, тогда что хочешь сделать?',

  // ===== DEEP WORK HANDLER =====

  // ⚠️ ВАЖНО: Лягушка говорит "я уверен" - МУЖСКОЙ РОД, не меняется!
  'deep_work_no_more_examples': 'Больше примеров нет - уверен, ты справишься!',

  'deep_work_congrats': {
    male: '<i>Ты проделал огромную работу! 🎉</i>\n\n' +
          'Осталось всего пару шагов 👣\n' +
          '<i>P.S. Не переживай, самая сложная часть позади\n' +
          'Перейдем к более приятной 😉</i>',
    female: '<i>Ты проделала огромную работу! 🎉</i>\n\n' +
            'Осталось всего пару шагов 👣\n' +
            '<i>P.S. Не переживай, самая сложная часть позади\n' +
            'Перейдем к более приятной 😉</i>',
  },

  // ===== ONBOARDING =====

  // ⚠️ ВАЖНО: "Я буду рад" - лягушка (М.Р.), не меняется!
  'onboarding_wait_evening': 'Отлично! Увидимся вечером 🌙\n\nА пока можешь написать мне о том, что сейчас чувствуешь или что происходит в твоей жизни. Я буду рад выслушать 💚',

  // ===== PRACTICE COMPLETED (fallback тексты) =====

  'practice_completed_1': 'Ты молодец! 🌟 Сегодня мы отлично поработали вместе.',

  'practice_completed_2': 'Отличная работа! 💚 Ты заботишься о себе, и это прекрасно.',

  'practice_completed_3': 'Супер! ✨ Каждая практика делает тебя сильнее.',

  'practice_completed_4': {
    male: 'Великолепно! 🌈 Ты сделал важный шаг для своего благополучия.',
    female: 'Великолепно! 🌈 Ты сделала важный шаг для своего благополучия.',
  },

  'practice_completed_5': {
    male: 'Ты справился! 🎯 На сегодня все задания выполнены.',
    female: 'Ты справилась! 🎯 На сегодня все задания выполнены.',
  },

  'practice_completed_6': 'Ты молодец! 🌙 Пора отдыхать.',

  'practice_completed_7': {
    male: 'Я горжусь тобой! 💫 Ты сделал отличную работу.',
    female: 'Я горжусь тобой! 💫 Ты сделала отличную работу.',
  },

  'practice_completed_8': 'Прекрасная работа! 🎉 Теперь можно расслабиться.',

  'practice_completed_9': 'Браво! 🌿 Все задания на сегодня завершены.',

  'practice_completed_10': {
    male: 'Замечательно! ⭐ Ты проявил заботу о себе.',
    female: 'Замечательно! ⭐ Ты проявила заботу о себе.',
  },
};

/**
 * Получает текст из словаря FIXED_TEXTS с учетом пола
 * @param key - Ключ текста в словаре
 * @param gender - Пол пользователя
 * @returns Текст или null если не найден
 */
function getFixedText(key: string, gender: 'male' | 'female' | 'unknown' | null): string | null {
  const text = FIXED_TEXTS[key];
  if (!text) return null;

  // Если текст - строка (одинаковый для обоих)
  if (typeof text === 'string') {
    return text;
  }

  // Если текст - объект с male/female
  if (gender === 'female' && text.female) {
    return text.female;
  }

  return text.male; // По умолчанию мужской род
}

/**
 * Проверяет, есть ли текст в словаре FIXED_TEXTS (по точному совпадению или ключу)
 * @param text - Текст для проверки
 * @returns Ключ текста в словаре или null
 */
function findFixedTextKey(text: string): string | null {
  // Проверяем точное совпадение с мужским вариантом
  for (const [key, value] of Object.entries(FIXED_TEXTS)) {
    if (typeof value === 'string') {
      if (value === text) return key;
    } else {
      if (value.male === text) return key;
    }
  }
  return null;
}

/**
 * Отправляет сообщение с автоматической адаптацией под пол пользователя
 *
 * Автоматически:
 * 1. Получает пол пользователя из БД (если userId передан)
 * 2. Проверяет словарь FIXED_TEXTS (приоритет)
 * 3. Если не найдено - адаптирует через gender-adapter (fallback)
 * 4. Заменяет {userName} на реальное имя
 * 5. Отправляет сообщение через Telegram API
 *
 * @param bot - Экземпляр Telegraf бота
 * @param chatId - ID чата для отправки
 * @param userId - ID пользователя для получения данных (пол, имя). Если null/undefined - отправка без адаптации
 * @param text - Текст сообщения
 * @param options - Опции для sendMessage (reply_parameters, parse_mode и т.д.)
 * @returns Promise с результатом отправки
 */
export async function sendToUser(
  bot: Telegraf,
  chatId: number,
  userId: number | null | undefined,
  text: string,
  options?: any
) {
  // Если userId не передан - отправляем как есть (админские уведомления, системные сообщения)
  if (!userId) {
    botLogger.debug(
      { chatId, textLength: text.length },
      'Отправка сообщения без адаптации (нет userId)'
    );
    return await bot.telegram.sendMessage(chatId, text, options);
  }

  // Получаем данные пользователя
  const user = getUserByChatId(userId);
  const userGender = (user?.gender || 'unknown') as 'male' | 'female' | 'unknown' | null;
  const userName = user?.name || null;

  botLogger.debug(
    {
      chatId,
      userId,
      userGender,
      userName,
      textLength: text.length
    },
    'Отправка сообщения пользователю с адаптацией'
  );

  let adaptedText: string;

  // ПРИОРИТЕТ 1: Проверяем словарь FIXED_TEXTS
  const fixedKey = findFixedTextKey(text);
  if (fixedKey) {
    const fixedText = getFixedText(fixedKey, userGender);
    if (fixedText) {
      adaptedText = fixedText;
      botLogger.debug({ fixedKey, userGender }, 'Использован готовый текст из FIXED_TEXTS');
    } else {
      // Fallback на gender-adapter
      adaptedText = adaptTextForGender(text, userGender);
    }
  } else {
    // ПРИОРИТЕТ 2: Fallback на gender-adapter для всех остальных текстов
    adaptedText = adaptTextForGender(text, userGender);
  }

  // Заменяем {userName} если есть в тексте
  if (userName) {
    adaptedText = adaptedText.replace(/\{userName\}/g, userName);
  }

  // Отправляем через стандартный API Telegram
  return await bot.telegram.sendMessage(chatId, adaptedText, options);
}
