/**
 * Миграция: ПОЛНАЯ ОЧИСТКА данных Лены (716928723)
 *
 * ПРИЧИНА:
 * Бот находится в стадии тестирования.
 * Нужно ПОЛНОСТЬЮ очистить ВСЕ данные Лены, чтобы она:
 * - Заново нажала /start
 * - Прошла весь онбординг
 * - Выбрала правильную timezone
 *
 * РЕШЕНИЕ:
 * Удаляем пользователя ПОЛНОСТЬЮ из БД.
 * При следующем /start создастся заново как новый пользователь.
 */

exports.up = async function(knex) {
  // ПОЛНОСТЬЮ УДАЛЯЕМ Лену из базы
  await knex('users')
    .where('chat_id', 716928723)
    .delete();
};

exports.down = function(knex) {
  // Откат: восстанавливаем ЛС
  return knex('users')
    .where('chat_id', 716928723)
    .update({
      dm_enabled: 1,
      channel_enabled: 0,
      channel_id: null
    });
};
