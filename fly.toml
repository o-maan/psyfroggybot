# fly.toml app configuration file generated for psy-froggy-bot on 2025-05-27T21:35:35+03:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'psy-froggy-bot'
primary_region = 'arn'

[mounts]
  source='froggy_data'
  destination='/data'
  processes = ['app']

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[checks]
  [checks.my_health_check]
    type = 'http'
    interval = '15s'
    timeout = '10s'
    grace_period = '30s'
    method = 'get'
    path = '/status'

    [checks.my_health_check.headers]
      Content-Type = 'application/json'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256

